# ADR-003：P4 Memory 检索的无向量基线

## 决策

MVP 不引入向量数据库或新依赖。P4 使用结构化作用域过滤、精确字段匹配与确定性关键词排序；未来向量索引只能作为可删除、可重建的可选投影，结果必须保留相同的来源、作用域和预算说明。

## 后果

离线、跨平台测试不依赖嵌入模型或网络；相关性能力保守但可复现。P4 API 保留查询签名和命中理由，后续可增加语义通道而不改变事实源或隔离规则。
