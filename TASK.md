# Bauhinia-Agent Evo 实施任务规划

> 状态：Active Planning v1.0
>
> 产品依据：Bauhinia_agent_PRD.md
>
> 文档属性：项目治理文档，纳入版本控制
>
> 历史产品文档如存在，仅作迁移参考；现由 Bauhinia_agent_PRD.md 作为产品事实源。

## 0. 使用方式

### 状态标记

- [ ] 尚未开始。
- [~] 正在进行；任一时刻只能有一个最小可交付任务处于此状态。
- [x] 已完成，且已满足本文件定义的验证与 Gate。
- [!] 阻塞；必须记录现象、证据、影响、已排除原因和所需决策。
- [-] 明确取消或移至未来版本；必须保留原因。

### 每项任务的执行纪律

开始前：

1. 阅读关联的 Evo PRD 需求、非目标和本阶段 Gate。
2. 执行 git status --short --branch，确认无意外用户变更。
3. 搜索已有的 Agent Loop、Session、Context、Tool、Permission、Skill、MCP 与测试接口。
4. 仅把一个最小任务标记为 [~]，记录范围、风险和验证方法。

完成前：

1. 运行与风险相称的自动化测试和必要的人工验收。
2. 检查正常、失败、拒绝、取消和超时路径。
3. 检查 Trace、记忆、权限和副作用影响。
4. 更新本文件的状态、验证证据与新增发现。
5. 提交前确认 Bauhinia_agent_PRD.md、TASK.md 与 AGENTS.md 的变更和当前工作范围一致。

## 1. 总体阶段与依赖

| 阶段 | 名称 | 主要产物 | 依赖 | MVP 关键 Gate |
| --- | --- | --- | --- | --- |
| P0 | 基线与设计冻结 | 可复现基线、ADR、评测样本 | 无 | 是 |
| P1 | Evo 领域与事件契约 | 版本化 Schema、Store、投影 | P0 | 是 |
| P2 | 显式规划与重规划 | PlanGraph、Decision Record、Replan | P1 | 是 |
| P3 | 分层记忆 | 记忆模型、作用域、冲突治理 | P1 | 是 |
| P4 | 检索与上下文编排 | Context Pack、来源与预算 | P2、P3 | 是 |
| P5 | 验证与失败归因 | Evidence、Outcome、错误分类 | P1、P2 | 是 |
| P6 | Experience Compiler | Experience Candidate、去重与审查 | P3、P5 | 是 |
| P7 | Skill/计划候选演化 | 版本化 Candidate、影子执行 | P2、P5、P6 | 是 |
| P8 | 评测与晋升门禁 | Eval Corpus、Promotion、回滚 | P6、P7 | 是 |
| P9 | Self Model 与适应策略 | 能力画像、不确定性、策略建议 | P4、P5、P8 | 否 |
| P10 | 多 Agent 协作演化 | 协作契约、汇总与隔离 | P2、P5、P8 | 否 |
| P11 | Evo TUI | 命令、状态、审查界面 | P2-P8 | 是 |
| P12 | Evolution Console GUI | 本地 API、Web 观察台 | P11 | 否 |
| P13 | 安全、可靠性与迁移 | 脱敏、恢复、性能、兼容 | P1-P12 | 是 |
| P14 | 文档、演示与发布 | 指南、示例、发布检查 | P13 | 是 |

## P0 — 基线、边界与评测样本

目标：在改变行为前建立可验证的 Bauhinia-Agent 基线，冻结第一轮产品边界与对照任务。

### 任务

- [x] P0-001：确认 Bauhinia-Agent Evo 的产品方向、MVP 边界与非目标。
  - 实施：以 Bauhinia_agent_PRD.md 为唯一产品依据；保留旧 PRODUCT.md，不删除历史内容。
  - 验证：PRD 包含核心闭环、功能需求、数据模型、验收指标、风险与路线图。

- [x] P0-002：建立版本化的项目治理与工程约束。
  - 实施：维护本文件、AGENTS.md 与 Bauhinia_agent_PRD.md，明确产品边界、任务 Gate 和提交复核规则。
  - 验证：三份文档由 Git 跟踪；提交前通过 git status --short 和 git diff --cached 复核其变更范围。

- [~] P0-003：盘点现有核心路径及其测试入口。
  - 实施：记录 Agent Loop、Session、Context L1-L4、Planner、Sub-agent、Tool Registry、Permission、MCP、Skill、Provider、TUI 的实际入口、数据流与测试命令；结果写入本地 ADR。
  - 验证：每项能力有代码位置、当前行为、可运行测试和 Evo 复用策略；没有以猜测替代证据。

- [ ] P0-004：建立离线的 Evo 基准任务集 v0。
  - 实施：构造至少 12 个可复现任务，覆盖新功能、小型缺陷、失败修复、跨文件改动、工具失败、权限拒绝、上下文冲突与恢复；每项保存输入、工作区基线、验收命令和期望证据类型。
  - 验证：任务可在无真实凭据条件下运行；至少 3 个任务可稳定失败，用于验证反思与修复路径。

- [ ] P0-005：记录可比较的基线指标。
  - 实施：对固定模型、Prompt、环境运行任务集，记录成功率、验证通过率、工具失败率、Token、耗时、人工干预、危险动作数与可复现性。
  - 验证：每个指标都有单位、采集命令、原始 Run ID 和环境摘要；指标不是单一黑盒总分。

### Gate P0

- 已知现有能力与接口有证据化盘点。
- 评测任务、环境和基线 Run 可复现。
- 未对现有 Agent Loop 做行为性改动。

## P1 — Evo 领域模型、事件协议与存储

目标：建立可演进、可迁移、可重建的事实与投影基础，而不是把 Evo 状态散落在 Prompt 或 UI 内存中。

### 任务

- [ ] P1-001：定义版本化 Evo 事件与标识符契约。
  - 实施：为 PlanCreated、PlanNodeUpdated、DecisionRecorded、EvidenceRecorded、OutcomeClassified、MemoryCreated、MemoryUsed、ExperienceCandidateCreated、EvaluationCompleted、PromotionChanged、SelfModelUpdated 定义显式模型、schema version、Run/Plan/Node/Memory/Candidate 关联 ID、UTC 时间与序号语义。
  - 验证：Schema 契约测试覆盖序列化、未知事件保留、旧版本读取和父子关系；稳定 Golden Event 序列可比较。

- [ ] P1-002：实现 append-only Evo Event Store 与重建投影。
  - 实施：在 .bauhinia-agent/evo 下保存原始事实；索引、统计和搜索状态仅作为可重建投影。关键元数据采用原子写入，损坏尾部可诊断并恢复。
  - 验证：删除投影后可由原始事件重建等价查询结果；Recorder 或 Store 故障不改变 Agent 最终语义，并产生可见诊断。

- [ ] P1-003：设计迁移、导入与兼容策略。
  - 实施：提供版本探测、降级读取、迁移日志和回滚策略；旧 Session 仍可打开，缺失 Evo 数据时显示无 Evo 数据而非失败。
  - 验证：旧会话、未知字段、部分损坏记录与跨 Windows 路径夹具均可处理。

- [ ] P1-004：编写 ADR-001：Evo 数据事实源与投影边界。
  - 实施：说明为什么原始事件是事实、记忆和索引是派生投影、何时可追加更正事件、何时绝不原地改写。
  - 验证：ADR 可映射到实现和测试，而非抽象口号。

### Gate P1

- 每一份持久化 Evo 数据可追溯至版本化原始事件。
- 任何派生索引都可清空重建。
- 旧 Session 与新 Evo 数据可并存且不阻断 CLI/TUI。

## P2 — 显式 Planning、Reasoning 摘要与 Replanning

目标：让计划、关键决策和调整可被检查，而非宣称保存或展示模型私有思维链。

### 任务

- [ ] P2-001：实现 PlanGraph 与 PlanNode。
  - 实施：支持目标、分解、依赖、前置条件、风险、预算、验证条件、状态、重试策略、父子关系与版本；复用现有 Planner 和 Agent Loop 的调度点。
  - 验证：单元测试覆盖 DAG 合法性、循环检测、状态迁移、依赖阻塞、取消与恢复。

- [ ] P2-002：实现结构化 Decision Record。
  - 实施：记录目标摘要、候选方案、选择、依据、假设、不确定性、预期证据、关联计划节点与时间；禁止持久化、要求或伪造完整隐藏 CoT。
  - 验证：记录可被用户理解并能链接到工具结果、测试、Diff；敏感模型原文和凭据不会进入记录。

- [ ] P2-003：接入 Replan 触发器与策略。
  - 实施：将验证失败、工具错误、权限拒绝、前提不成立、上下文冲突、预算耗尽和用户变更转成显式触发事件；支持继续、缩小、替代、求助、终止五类安全结果。
  - 验证：固定任务中至少覆盖测试失败、工具失败、权限拒绝三种重规划；每次重规划有触发、前后版本和原因。

- [ ] P2-004：定义 Planner、Executor、Verifier 的协作边界。
  - 实施：明确输入、输出、权限、失败语义与汇总契约；不复制第二套 Agent Loop。
  - 验证：集成测试验证执行器不能绕过验证器或权限层；计划状态与 Session 状态一致。

### Gate P2

- 一个多步骤任务可查看其计划图、关键决策和至少一次重规划证据。
- 计划错误不会直接变成未受控的工具调用。

## P3 — 分层长期记忆与生命周期治理

目标：把记住变为来源明确、按作用域隔离、可失效和可审查的能力。

### 任务

- [ ] P3-001：定义五层记忆模型与写入规则。
  - 实施：区分 Working/Context（复用现有 L1-L4）、Task/Session、Episodic、Semantic、Procedural、Meta-memory；规定每层可存内容、最大生存期和读写权限。
  - 验证：模型测试拒绝无来源、无作用域、无置信度或把临时对话直接升格为长期事实的写入。

- [ ] P3-002：实现 Memory Record、冲突和失效协议。
  - 实施：每条记录含来源 Run/Evidence、项目和用户作用域、时间、置信度、TTL、冲突组、状态、敏感级别；支持 supersede、invalidate、merge proposal 和人工确认。
  - 验证：两个冲突项目事实不会静默拼接；过期记录降权；用户确认可留下明确审计轨迹。

- [ ] P3-003：实现记忆事实源和可重建检索索引。
  - 实施：原始记忆事件 append-only；文本、向量、SQLite 索引为投影并可选依赖；不让索引反向覆盖事实。
  - 验证：重建索引后结果等价；无向量依赖时仍有确定性关键词和元数据检索降级。

- [ ] P3-004：实现记忆隔离与用户控制。
  - 实施：默认项目隔离；工作区、用户、组织（未来）须显式选择；提供查看、失效、导出元数据与禁用记忆写入能力。
  - 验证：跨项目夹具不会被召回；禁用写入后不会产生新增长期记忆事件。

### Gate P3

- 每个长期记忆都能回答：来自哪里、适用于哪里、为何可信、何时失效。
- 记忆失效不会篡改历史 Run。

## P4 — Retrieval 与 Context Pack 编排

目标：在有限上下文中选择高价值经验，并把使用了什么变成可检查结果。

### 任务

- [ ] P4-001：实现 Query Signature 与多通道检索。
  - 实施：用任务目标、仓库特征、错误类型、工具环境、计划节点和约束构建查询；组合精确匹配、结构化过滤、关键词和可选语义检索。
  - 验证：离线夹具证明相关经验排在冲突或跨项目经验之前；排序具有确定性或明确随机种子。

- [ ] P4-002：实现预算感知的 Context Pack。
  - 实施：按相关度、可信度、新鲜度、作用域、冲突和 Token 成本选片段；每片包含来源、截断信息与纳入理由。
  - 验证：严格 Token 上限下仍保留最相关证据；超预算时有可解释的舍弃记录。

- [ ] P4-003：记录记忆使用后的结果反馈。
  - 实施：将 MemoryUsed 与计划、执行、验证结果关联，用作将来排序信号；不把一次成功误判为因果结论。
  - 验证：成功、失败和未使用三类记忆均能在 Run 后区分；不因单次结果自动提升为 Promoted。

### Gate P4

- 用户可检查某一计划节点获得的 Context Pack、来源与 Token 预算。
- 记忆检索不会突破项目隔离或上下文上限。

## P5 — Verification、Outcome 与失败归因

目标：让学习来源于可验证的结果，而不是模型自我评价。

### 任务

- [ ] P5-001：统一 Evidence Adapter。
  - 实施：接入测试、lint、类型检查、构建、Diff、工具返回、权限事件、用户确认与可选人工标注；每条证据记录命令或输入摘要、cwd、退出码、时间和脱敏结果。
  - 验证：测试、工具、权限三类证据均可在同一 Run 查询；真实凭据不会落盘。

- [ ] P5-002：实现 Outcome Classifier 与失败分类。
  - 实施：区分任务失败、验证失败、工具或环境失败、权限拒绝、取消、超时、评测基础设施失败与未知；维护证据链和置信度。
  - 验证：故意制造的测试失败、无效工具参数、网络不可用、权限拒绝被稳定分类，且不混为模型失败。

- [ ] P5-003：实现最小诊断摘要。
  - 实施：输出失败位置、可观察现象、候选原因、已排除项、下一步验证与不确定性；供 Replan 和 Experience Compiler 使用。
  - 验证：诊断能链接原始证据，并拒绝无证据的确定性断言。

### Gate P5

- 每个被学习的成功或失败都有至少一种可追溯证据。
- 失败原因、环境故障和权限拒绝可分别统计。

## P6 — Experience Compiler

目标：从已验证轨迹提炼候选经验，避免把完整会话噪声直接塞回 Prompt。

### 任务

- [ ] P6-001：定义 Experience Candidate 生成策略。
  - 实施：输入 Plan、Decision、Evidence、Outcome 与环境摘要；输出稳定事实、策略模板、反模式或调试提示候选，附来源集合、适用条件、反例、置信度和新颖度。
  - 验证：成功与失败轨迹都能生成候选；候选可回链至所有源证据。

- [ ] P6-002：实现去重、聚类与冲突检测。
  - 实施：根据作用域、任务特征、语义或结构相似度和结论相容性分组；只生成合并提案，不静默覆盖既有经验。
  - 验证：重复运行不会线性制造近似记忆；相反结论形成显式冲突组。

- [ ] P6-003：实现候选审查队列与人工编辑。
  - 实施：允许接受、拒绝、延后、修改作用域、TTL、敏感级别并留下审计事件；默认不自动进入长期 Semantic 或 Procedural 层。
  - 验证：未经接受的候选不会进入常规检索；拒绝原因可反向用于改进编译规则。

### Gate P6

- 可从一个真实 Run 生成、审查并追溯一个经验候选。
- 学习过程不依赖保存完整隐藏推理或未经验证的自评。

## P7 — Skill、计划模板与策略候选演化

目标：让可复用做法以受约束的版本化候选形式成长，而不是让 Agent 直接自改核心。

### 任务

- [ ] P7-001：定义 Evolution Candidate 与版本清单。
  - 实施：支持 Plan Template、Skill Draft、Tool Invocation Policy、Memory Rule 四类候选；声明输入输出、依赖、Effect、适用范围、风险、来源 Run、版本和状态。
  - 验证：Candidate Schema 对未知 Effect 默认高风险；所有候选可导出最小元数据但不含密钥。

- [ ] P7-002：实现从重复成功或失败模式生成候选。
  - 实施：设置最小证据阈值、互异任务要求和失败反例；从 Experience Candidate 推导，而非从单次模型输出直接生成。
  - 验证：单次成功不能产生可晋升候选；至少三条独立已验证轨迹可产生可审查草案。

- [ ] P7-003：实现影子使用和受控试验。
  - 实施：Candidate 默认仅建议或 Shadow 对比；真实工具副作用仍经现有 Permission Engine；失败可禁用、回滚和保留证据。
  - 验证：Candidate 不能改变权限、绕过确认、修改模型权重、替换 Agent Loop 或自动写入核心源码。

### Gate P7

- 一个程序化经验能成为可版本化、可禁用、可追溯的候选。
- 未晋升候选不会默认改变真实任务结果。

## P8 — Eval Harness、晋升门禁与回滚

目标：将自我改进从自我宣称变成在固定保留集上可比较、可否决、可回滚的流程。

### 任务

- [ ] P8-001：实现 Eval Case、Variant、Trial 与 Evaluator 协议。
  - 实施：固定任务输入、工作区基线、环境、模型配置、候选版本、评测器版本与随机种子；支持重复 Trial。
  - 验证：同一 Trial 可复跑并打开为标准 Run；评测失败与任务失败状态不同。

- [ ] P8-002：建立 held-out 评测集与防污染机制。
  - 实施：将训练或经验来源、开发集和保留集隔离；评测器输入有版本和哈希；禁止候选读取保留集答案。
  - 验证：审计测试证明来源 Run 不与对应 held-out Case 重合；泄漏时 Trial 标记无效。

- [ ] P8-003：实现 Candidate 生命周期和 Promotion Gate。
  - 实施：状态为 Candidate 到 Shadow 到 Validated 到 Promoted 到 Deprecated 或 Rejected；比较成功率、验证质量、成本、时延、风险事件和样本数，设降级阈值与人工审批点。
  - 验证：候选仅在保留集改进且安全和成本未超过阈值时可晋升；低样本或高不确定性必须停留在 Shadow 或 Validated。

- [ ] P8-004：实现回滚与反奖励投机检查。
  - 实施：按版本回滚并保留影响范围；检测跳过测试、减少验证、伪造成功、缩短输出掩盖失败等指标异常。
  - 验证：模拟劣化候选自动拒绝或回滚；历史 Run 保持可读取。

### Gate P8

- 至少一个候选完成从 Candidate 到 Promoted 或 Rejected 的完整、可审计流程。
- 改进结论来自 held-out 对比，不是单次演示。

## P9 — Self Model 与保守适应

目标：形成透明的能力画像，用于调整规划与验证力度，而不授予 Agent 新权限。

### 任务

- [ ] P9-001：实现任务分类、可靠性与不确定性画像。
  - 实施：按语言、仓库规模、任务类型、工具类别、风险和验证充分度聚合；保留样本量、时间窗、置信区间或不确定性。
  - 验证：低样本情形显示不足以判断；不同项目或模型配置不被错误合并。

- [ ] P9-002：实现 Policy Suggestion。
  - 实施：允许建议更细计划、更多验证、拆分任务、询问用户或降级到保守模板；所有建议可解释、可关闭、可回放。
  - 验证：高失败类别触发风险提示和更严格验证，但不会静默扩大工具、网络或写入权限。

### Gate P9

- 用户能看到系统认为擅长或不擅长什么、基于哪些样本、置信度如何。
- Self Model 不成为黑盒权限引擎。

## P10 — 多 Agent 协作与经验归并

目标：把现有 Sub-agent 能力接入同一计划、权限、证据和记忆治理，而非增加不可追溯的并发角色。

### 任务

- [ ] P10-001：定义角色和交接契约。
  - 实施：Planner、Researcher、Executor、Verifier、Critic、Curator 使用统一 Task Contract，包含目标、输入快照、可用能力、预算、输出证据、置信度和取消语义。
  - 验证：任何子 Agent 输出若无证据，不能直接成为长期记忆或晋升依据。

- [ ] P10-002：实现并发、冲突与汇总策略。
  - 实施：限制并发、资源、上下文与写入竞争；冲突结论保留分支并由 Verifier 或 Curator 处理；父 Run 聚合子 Run 引用。
  - 验证：并发夹具下无重复副作用、无共享状态损坏；取消能回收子任务。

- [ ] P10-003：接入跨 Agent 经验归并。
  - 实施：按来源角色和独立性标注证据，避免同一错误被多次计票。
  - 验证：多个相互复制的子 Agent 结论不会被误判为多份独立验证。

### Gate P10

- 多 Agent 的计划、权限和结果可在一个父 Run 内追溯。
- 并发不会绕过 Effect 或 Permission 规则。

## P11 — Evo TUI（MVP 用户界面）

目标：在既有 Textual TUI 中提供可读、可操作的 Evo 能力，优先完成审查和控制而非复制复杂图形界面。

### 任务

- [ ] P11-001：实现 Plan 与 Evidence 视图。
  - 实施：展示计划树、节点状态、依赖、重规划、决策摘要、证据和失败归因；支持键盘导航、窄终端与无颜色降级。
  - 验证：可从一个 Run 定位失败节点并打开关联证据；状态不只依赖颜色。

- [ ] P11-002：实现 Memory 与 Candidate 审查流。
  - 实施：提供查看来源、作用域、置信度、冲突、TTL、接受、拒绝、失效和 Candidate 生命周期操作；每个真实动作走统一权限和命令路径。
  - 验证：用户可阻止候选进入检索，也能找到一个记忆为何被使用。

- [ ] P11-003：实现 Evo 命令与帮助。
  - 实施：遵循现有命令注册风格，至少提供计划查看、记忆查询、候选列表、评测状态、Self Model 摘要和安全禁用命令。
  - 验证：帮助可发现，错误参数有字段级诊断，旧 TUI 流程不回归。

### Gate P11

- 用户无需读取原始 JSONL，即可完成观察计划、查看证据、审查经验、决定晋升或拒绝的 MVP 流程。

## P12 — Evolution Console GUI（后续扩展）

目标：在不复制领域逻辑的前提下，提供适合大量事件、对比和图形化审查的本地 Web 界面。

### 任务

- [ ] P12-001：设计本地 Studio API 与事件流。
  - 实施：仅监听 localhost；Schema 校验 API；按 Run ID 和 sequence 增量查询；领域服务位于 Python 核心而非 React。
  - 验证：API 契约测试、断线重连和权限命令统一；GUI 关闭不取消 Run。

- [ ] P12-002：实现 Run、Evidence、Memory、Candidate 页面。
  - 实施：支持事件虚拟列表、Plan Graph、差异比较、来源跳转、筛选、复制、搜索和 URL 可恢复状态。
  - 验证：10,000 以上事件基准可交互；Loading、Empty、Error、Disconnected 完整。

- [ ] P12-003：实现评测对比与版本回滚界面。
  - 实施：可比较 Variant 和 Trial 指标与证据，展示不确定性和安全差异，提供审查式晋升或回滚。
  - 验证：界面不使用单一总分掩盖成功、成本、时延和风险差异。

### Gate P12

- GUI 只是同一 Evo 内核的观察与控制面，不成为第二套业务系统。

## P13 — 安全、可靠性、性能与迁移

目标：使 Evo 的持久化、学习和试验能力在真实开发环境中可控、可恢复且不泄露敏感信息。

### 任务

- [ ] P13-001：实施脱敏、数据最小化与 Bundle 安全审计。
  - 实施：在持久化边界清除 API Key、Token、Cookie、认证头、私钥和敏感环境；导出前二次扫描；未知外部内容默认不可信。
  - 验证：密钥夹具不出现在事件、记忆、索引、UI、日志或导出物中。

- [ ] P13-002：实施副作用隔离与取消恢复。
  - 实施：复用 Permission Engine；标注 Tool Effect；可写操作使用隔离工作区；取消回收子进程、临时目录和 worktree。
  - 验证：拒绝、超时、取消和未知 Effect 均安全；不会因 Evo 候选绕过权限。

- [ ] P13-003：性能与容量基准。
  - 实施：测量 Event Recorder 开销、检索延迟、索引重建、内存占用、TUI 和 GUI 事件量；给出可接受预算与退化策略。
  - 验证：Trace 或 Evo 关闭时原执行开销近似基线；高事件量不阻塞 Agent Loop。

- [ ] P13-004：Windows、恢复和迁移验收。
  - 实施：测试路径、文件锁、编码、进程取消、SQLite 或文件恢复和旧数据迁移；记录平台差异。
  - 验证：Windows CI 或同等人工验收可复现，迁移失败可诊断且不破坏原始事实。

### Gate P13

- 安全夹具、恢复夹具、容量基准和兼容性测试通过。
- 不存在默认外部监听、明文凭据持久化或未受控副作用路径。

## P14 — 文档、演示与发布

目标：将 Evo 的能力、局限、验证方式和维护方式交付给真实使用者与贡献者。

### 任务

- [ ] P14-001：编写用户与开发者文档。
  - 实施：说明安装、开关、计划、记忆、候选、评测流程、隐私、数据目录、故障诊断和不支持的能力；为事件与 Candidate 提供最小示例。
  - 验证：全新环境按文档能完成一个离线 Evo 任务。

- [ ] P14-002：准备端到端演示与回归场景。
  - 实施：展示失败、证据、反思候选、Shadow 对比、晋升或拒绝完整路径，并同时展示一个失败或拒绝候选。
  - 验证：演示使用公开或合成数据，无真实凭据，可从空目录复现。

- [ ] P14-003：发布前检查。
  - 实施：检查版本、迁移说明、许可证、新依赖、性能、安全、文档链接与所有本地规划文件排除状态。
  - 验证：完成发布清单；无未解释的测试跳过或已知高风险回归。

### Gate P14 / MVP 完成标准

- 用户可观察并控制完整 Evo 闭环：检索经验、制定计划、执行并验证、生成经验候选、在评测后晋升或拒绝。
- 每个关键结论有来源证据、作用域和版本。
- 现有 Bauhinia-Agent CLI、TUI、会话、权限、Tools、MCP、Skills 基础能力未被破坏。
- 没有模型权重训练、自动修改核心代码、自动权限升级或未评测自动晋升。

## 2. 关键设计决策待办

以下事项必须在对应实现开始前写 ADR 或取得明确决定：

- [ ] D-001：Evo Event Store 的文件格式、SQLite 投影边界与锁策略。
- [ ] D-002：Memory 检索是否引入可选向量数据库，以及无向量降级方案。
- [ ] D-003：Decision Record 的最小字段与隐私红线。
- [ ] D-004：离线 Eval Corpus 的许可、存储、更新和 held-out 防污染流程。
- [ ] D-005：Candidate 晋升的默认阈值、人工审批角色和回滚 SLA。
- [ ] D-006：Skill Draft 的格式如何兼容现有 bauhinia_agent/skills 发现和加载机制。
- [ ] D-007：多 Agent 角色是否先采用现有实现的最小包装，还是新增领域协议。
- [ ] D-008：GUI 是否列入 v1.0，及其 FastAPI、React 打包与本地监听策略。

## 3. 当前执行队列

下一项最小可交付任务应为：

- [ ] P0-003：盘点现有核心路径及其测试入口。

在它开始前，不应提前实现 Evo 代码或做大规模重构。
